# 验证记录

日期：2026-09-14。本机 macOS 开发环境，所有 VPN 测试均使用临时 fake 数据和模拟命令。

## 已验证

- Go 测试：用户名及端点注入校验、CIDR 规范化/覆盖/保留网段、status-version 3 正常/截断/过期/非法流量字段。
- HTTP 集成：初始化一次性关闭、登录与失败锁定、未授权拦截、CSRF、会话过期、改密后会话失效。
- fake 闭环：创建、下载、备注及配置元数据、重复用户、路由批量反馈、revision 冲突和幂等应用、在线时间记录、撤销及禁止下载、重载 JSON 保留状态。
- 故障测试：创建失败、撤销/CRL 安装失败后阻断下载及重试、路由重启失败回滚、遗留路由备份恢复、文件原子替换、拒绝符号链接、跨进程文件锁。
- 运行 `go test -race ./...`、`go vet ./...`、前端 `vue-tsc --noEmit` 和 Vite 生产构建。
- Linux amd64 / arm64 无 CGO 二进制交叉编译。
- 浏览器桌面实际操作：初始化页展示、测试账号登录、仪表盘、创建 `macbook` 模拟客户端、批量路由规范化、拒绝 `0.0.0.0/0`、应用二次确认、版本更新与重连提示。
- `npm audit` 包括开发依赖，升级 Vite 至 6.4.3 后报告 0 个已知漏洞。
- 安装脚本 `bash -n` 语法检查。

## 尚需真实环境验收

- Ubuntu 22.04 / 24.04 的 sudoers、systemd 沙箱、EasyRSA 和服务器固定目录/属主/权限实际兼容性。
- OpenVPN Connect 导入真实客户端配置、握手连接、客户端分流及本地出口。
- 实际 CRL 签名和安装、撤销后的客户端重连拒绝。
- 真实服务故障后的路由回滚及宿主机重启后的恢复。
- 手机/平板浏览器完整交互。

构建有单个前端 JS chunk 超过 Vite 默认 500 kB 阈值的提示（gzip 约 185 kB），不影响构建与单二进制部署。未执行任何真实服务器安装、服务重启或证书撤销。

## ovpn-cli 更新功能

已补充并通过更新专项测试：Release/资产地址校验、SHA256 损坏及重复条目拒绝、预发行版本拒绝、HTTPS 跳转限制、跨域移除 GH_TOKEN、自动更新不降级、主程序/helper 同步替换、保持原停止状态、PKI 占锁时不替换、启动失败回滚、中断事务恢复，以及回滚失败时保留备份。

版本号注入已验证，带 `v0.1.0` 构建标记的本地临时二进制可正确输出版本；这不代表已发布 v0.1.0。Release workflow 已做 YAML 语法检查，安装脚本通过 bash 语法检查。没有执行 GitHub Actions 发版，也没有操作真实 Ubuntu 服务做在线升级。
